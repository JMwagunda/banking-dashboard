{
  "name": "solution-implementation-analyst-assessment",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "banking_dashboard"
  ],
  "scripts": {
    "dev": "npm --prefix banking_dashboard run dev",
    "build": "npm --prefix banking_dashboard run build",
    "preview": "npm --prefix banking_dashboard run preview",
    "lint": "npm --prefix banking_dashboard run lint",
    "start": "npm --prefix banking_dashboard run dev"
  }
}
